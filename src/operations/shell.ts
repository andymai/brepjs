/**
 * Standalone shell (hollowing) operations.
 * Ported from replicad's shapes.ts _3DShape.shell method.
 */

// Re-exported from topology/shapes.ts where the method lives on _3DShape.
// The actual shell logic is on the class method since it needs this.oc.
// This file exists for the layer structure.
// See topology/shapes.ts for the implementation.

export {};
